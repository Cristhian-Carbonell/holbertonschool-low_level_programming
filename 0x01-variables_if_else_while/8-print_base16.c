#include <stdio.h>

/**
 * main -prints all the numbers of base 16 in lowercase
 *
 * Return: 0
 */
int main(void)
{
	int b;

	for (b = '0'; b <= '9'; b++)
		putchar(b);
	for (b = 'a'; b <= 'f'; b++)
		putchar(c);
	putchar('\n');
	return (0);
}
